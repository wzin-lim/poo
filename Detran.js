class veículo{

constructor(marca,modelo,ano){
this.marca=marca    
this.modelo=modelo
this.ano=ano

    let marca=prompt
    let modelo=prompt
    let ano=prompt
alert($(this.marca) ($(this.modelo))($(this.ano)))
}
}
class Carro extends veiculo {
    super(marca,modelo,ano){
    constructor(hp)
this.hp=hp
let hp=parseInt(prompt("Qual a potencia?"))
    }
    }
class Moto extends veiculo {
super (marca,modelo,ano){
        constructor(cc)
this.cc=cc
let cc=parseInt(prompt("Quantas cilindradas?"))
}
} 


switch (op) {
    case '1' :
    var marca =prompt
    let modelo = prompt
    let ano = prompt
    let potencia = prompt
    const carro1 = new(ano(marca,modelo,ano,potencia));
    alert ('${carro1.marca}\n${carro1.modelo}\n${carro1.ano}\n');
    break 

}
